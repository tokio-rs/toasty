[package]
name = "tests-fixture-cdylib-ctor"
version = "0.0.0"
authors.workspace = true
edition.workspace = true
license.workspace = true
readme.workspace = true
publish = false

# Smoke-test fixture for `docs/dev/design/standalone-cli.md`. Declared as a
# plain `lib` (default crate-type = ["lib"]); the smoke test rebuilds it as a
# `cdylib` via `cargo rustc --crate-type cdylib` to verify that constructors
# survive the override on every supported platform.

[dependencies]
linktime = "0.14"
