model Person {
    #[key]
    #[auto]
    id: Id,

    #[index]
    parent_id: Option<Id<Person>>,

    #[relation(key = parent_id, references = id)]
    parent: Option<Person>,

    children: [Person],
}
